name: CPython Android runtime cache

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Cached phase
        run: echo "CPython 3.14.6 ARM runtimes are cached. Use the Python macro APK workflow."
