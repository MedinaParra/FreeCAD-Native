name: Native device APK

on:
  workflow_dispatch:
  push:
    branches: [native-device-test]
  pull_request:
    branches: [main]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.3.1'
      - uses: android-actions/setup-android@v3
      - name: Install Android NDK and CMake
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-36" "build-tools;36.0.0" "platform-tools" "ndk;28.2.13676358" "cmake;3.22.1"
      - name: Compile universal native APK
        run: |
          set -o pipefail
          gradle :app:assembleDebug --stacktrace 2>&1 | tee native-build.log
      - name: Report native build failure
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.existsSync('native-build.log')
              ? fs.readFileSync('native-build.log', 'utf8')
              : 'native-build.log was not created';
            const lines = log.split(/\r?\n/);
            const selected = lines.filter(line => /(^e:| error:|unresolved reference|cannot access|compilation error|MainScreenNative|NativeBoxBridge|native_box\.cpp|CMake Error)/i.test(line));
            const report = (selected.length ? selected : lines.slice(-80)).slice(-160).join('\n');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `## Native compiler diagnostics\n\n\`\`\`text\n${report}\n\`\`\``
            });
      - name: Upload native build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: native-build-log
          path: native-build.log
          if-no-files-found: warn
      - name: Verify ARM native libraries
        run: |
          unzip -l app/build/outputs/apk/debug/app-debug.apk | tee apk-contents.txt
          grep -q "lib/armeabi-v7a/libfreecad_android_core.so" apk-contents.txt
          grep -q "lib/arm64-v8a/libfreecad_android_core.so" apk-contents.txt
          grep -q "lib/armeabi-v7a/libc++_shared.so" apk-contents.txt
          grep -q "lib/arm64-v8a/libc++_shared.so" apk-contents.txt
          cp app/build/outputs/apk/debug/app-debug.apk FreeCAD-Native-Universal-v0.3.1.apk
      - uses: actions/upload-artifact@v4
        with:
          name: freecad-native-universal-apk
          path: FreeCAD-Native-Universal-v0.3.1.apk
          if-no-files-found: error
